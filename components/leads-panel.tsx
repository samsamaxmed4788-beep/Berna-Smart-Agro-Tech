'use client'

import { useMemo, useState } from 'react'
import { createLead, updateLeadStatus } from '@/app/actions/leads'
import { Plus, UserRound, X } from 'lucide-react'

type Lead = { id: string; firstName: string; lastName: string | null; email: string | null; company: string | null; status: string; score: number; valueCents: number }
const statuses = ['new', 'contacted', 'qualified', 'proposal', 'won', 'lost'] as const
const labels: Record<string, string> = { new: 'New', contacted: 'Contacted', qualified: 'Qualified', proposal: 'Proposal', won: 'Won', lost: 'Lost' }

export function LeadsPanel({ initialLeads = [] }: { initialLeads?: Lead[] }) {
  const [leads, setLeads] = useState(initialLeads); const [filter, setFilter] = useState('all'); const [open, setOpen] = useState(false); const [saving, setSaving] = useState(false); const [error, setError] = useState('')
  const visible = filter === 'all' ? leads : leads.filter((lead) => lead.status === filter)
  const total = useMemo(() => leads.reduce((sum, lead) => sum + lead.valueCents, 0), [leads]); const won = useMemo(() => leads.filter((lead) => lead.status === 'won').reduce((sum, lead) => sum + lead.valueCents, 0), [leads])
  async function move(id: string, status: typeof statuses[number]) { await updateLeadStatus(id, status); setLeads((items) => items.map((lead) => lead.id === id ? { ...lead, status } : lead)) }
  async function submit(formData: FormData) { setSaving(true); setError(''); try { await createLead({ firstName: String(formData.get('firstName') || ''), lastName: String(formData.get('lastName') || ''), email: String(formData.get('email') || ''), company: String(formData.get('company') || ''), source: String(formData.get('source') || ''), valueCents: Number(formData.get('valueCents') || 0) * 100 }); setOpen(false); window.location.reload() } catch (e) { setError(e instanceof Error ? e.message : 'Could not create lead') } finally { setSaving(false) } }
  return <section className="space-y-4"><div className="flex flex-wrap items-end justify-between gap-3"><div><p className="font-mono text-[10px] uppercase tracking-[0.18em] text-muted-foreground">Revenue engine</p><h2 className="mt-1 text-lg font-semibold">Leads & CRM</h2><p className="mt-1 text-xs text-muted-foreground">Drag opportunities across the pipeline or use the status filter.</p></div><button onClick={() => setOpen(true)} className="flex items-center gap-2 rounded-md bg-primary px-3 py-2 text-xs font-medium text-primary-foreground"><Plus className="size-3.5" />Add lead</button></div><div className="grid gap-3 sm:grid-cols-3"><Metric label="Pipeline value" value={`$${(total / 100).toLocaleString()}`} /><Metric label="Won value" value={`$${(won / 100).toLocaleString()}`} /><Metric label="Open deals" value={String(leads.filter((lead) => !['won', 'lost'].includes(lead.status)).length)} /></div><div className="flex flex-wrap gap-2">{['all', ...statuses].map((value) => <button key={value} onClick={() => setFilter(value)} className={`rounded-md border px-3 py-1.5 text-xs ${filter === value ? 'border-primary bg-primary/10 text-primary' : 'border-border text-muted-foreground hover:bg-accent'}`}>{value === 'all' ? 'All deals' : labels[value]}</button>)}</div><div className="grid gap-3 overflow-x-auto pb-2 md:grid-cols-3 xl:grid-cols-6">{statuses.map((status) => <div key={status} onDragOver={(event) => event.preventDefault()} onDrop={(event) => { const id = event.dataTransfer.getData('leadId'); if (id) void move(id, status) }} className="min-h-52 min-w-48 rounded-xl border border-border bg-card p-3"><div className="mb-3 flex items-center justify-between"><span className="font-mono text-[10px] uppercase tracking-widest text-muted-foreground">{labels[status]}</span><span className="rounded bg-accent px-1.5 py-0.5 font-mono text-[10px] text-primary">{leads.filter((lead) => lead.status === status).length}</span></div><div className="space-y-2">{visible.filter((lead) => lead.status === status).map((lead) => <article key={lead.id} draggable onDragStart={(event) => event.dataTransfer.setData('leadId', lead.id)} className="cursor-grab rounded-lg border border-border bg-background p-3 active:cursor-grabbing"><div className="flex items-start gap-2"><div className="flex size-7 shrink-0 items-center justify-center rounded-full bg-accent text-primary"><UserRound className="size-3.5" /></div><div className="min-w-0"><p className="truncate text-xs font-medium">{lead.firstName} {lead.lastName || ''}</p><p className="truncate text-[10px] text-muted-foreground">{lead.company || lead.email || 'No details'}</p></div></div><div className="mt-3 flex items-center justify-between font-mono text-[10px]"><span className="text-muted-foreground">Score {lead.score}</span><span className="text-primary">${(lead.valueCents / 100).toLocaleString()}</span></div></article>)}</div></div>)}</div>{open && <div className="fixed inset-0 z-50 flex items-center justify-center bg-background/80 p-4 backdrop-blur-sm"><form action={submit} className="w-full max-w-md space-y-4 rounded-xl border border-border bg-card p-6 shadow-2xl"><div className="flex items-center justify-between"><h3 className="font-semibold">Add lead</h3><button type="button" aria-label="Close" onClick={() => setOpen(false)}><X className="size-4" /></button></div>{error && <p className="text-xs text-destructive">{error}</p>}<div className="grid grid-cols-2 gap-3"><input name="firstName" required placeholder="First name" className="rounded-md border border-border bg-background px-3 py-2 text-sm" /><input name="lastName" placeholder="Last name" className="rounded-md border border-border bg-background px-3 py-2 text-sm" /></div><input name="email" type="email" placeholder="Email" className="w-full rounded-md border border-border bg-background px-3 py-2 text-sm" /><input name="company" placeholder="Company" className="w-full rounded-md border border-border bg-background px-3 py-2 text-sm" /><input name="valueCents" type="number" min="0" placeholder="Opportunity value" className="w-full rounded-md border border-border bg-background px-3 py-2 text-sm" /><button disabled={saving} className="w-full rounded-md bg-primary px-3 py-2.5 text-sm font-medium text-primary-foreground">{saving ? 'Saving…' : 'Create lead'}</button></form></div>}</section>
}
function Metric({ label, value }: { label: string; value: string }) { return <div className="rounded-xl border border-border bg-card p-4"><p className="font-mono text-[10px] uppercase tracking-widest text-muted-foreground">{label}</p><p className="mt-3 text-2xl font-semibold">{value}</p></div> }
