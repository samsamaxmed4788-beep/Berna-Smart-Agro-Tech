'use client'

import { useState } from 'react'
import { CalendarDays, Mail, Megaphone, Plus, Search, Share2, Video } from 'lucide-react'
import { createCampaign, createCalendarItem } from '@/app/actions/marketing'

const channels = [{ id: 'email', label: 'Email', icon: Mail }, { id: 'social', label: 'Social', icon: Share2 }, { id: 'search', label: 'Search', icon: Search }, { id: 'content', label: 'Content', icon: Megaphone }, { id: 'webinar', label: 'Webinar', icon: Video }]
const days = ['Mon 12', 'Tue 13', 'Wed 14', 'Thu 15', 'Fri 16']

export function MarketingPanel() {
  const [campaignOpen, setCampaignOpen] = useState(false)
  const [itemOpen, setItemOpen] = useState(false)
  const [message, setMessage] = useState('')
  const [campaigns, setCampaigns] = useState([{ name: 'Q3 demand sprint', status: 'Active', budget: '$12,400', reach: '48.2k' }, { name: 'Founder signal series', status: 'Scheduled', budget: '$4,800', reach: '—' }])
  const [items, setItems] = useState([{ title: 'Customer proof thread', channel: 'social', day: 'Tue 13' }, { title: 'Pipeline playbook', channel: 'content', day: 'Wed 14' }, { title: 'Growth memo #08', channel: 'email', day: 'Fri 16' }])
  async function addCampaign(formData: FormData) { try { await createCampaign({ name: String(formData.get('name') || ''), objective: String(formData.get('objective') || ''), budgetCents: Number(formData.get('budget') || 0) * 100 }); setCampaigns((v) => [{ name: String(formData.get('name')), status: 'Draft', budget: `$${Number(formData.get('budget') || 0).toLocaleString()}`, reach: '—' }, ...v]); setCampaignOpen(false); setMessage('Campaign saved') } catch (e) { setMessage(e instanceof Error ? e.message : 'Could not save campaign') } }
  async function addItem(formData: FormData) { try { const channel = String(formData.get('channel')); await createCalendarItem({ title: String(formData.get('title') || ''), channel, scheduledFor: String(formData.get('scheduledFor') || '') }); setItems((v) => [...v, { title: String(formData.get('title')), channel, day: 'Fri 16' }]); setItemOpen(false); setMessage('Calendar item saved') } catch (e) { setMessage(e instanceof Error ? e.message : 'Could not save item') } }
  return <section className="space-y-5"><div className="flex flex-wrap items-end justify-between gap-3"><div><p className="font-mono text-[10px] uppercase tracking-[0.18em] text-muted-foreground">Growth orchestration</p><h2 className="mt-1 text-lg font-semibold">Marketing engine</h2><p className="mt-1 text-xs text-muted-foreground">Plan campaigns, channels, and content from one operating surface.</p></div><div className="flex gap-2"><button onClick={() => setItemOpen(true)} className="flex items-center gap-2 rounded-md border border-border px-3 py-2 text-xs hover:bg-accent"><CalendarDays className="size-3.5" />Plan content</button><button onClick={() => setCampaignOpen(true)} className="flex items-center gap-2 rounded-md bg-primary px-3 py-2 text-xs font-medium text-primary-foreground"><Plus className="size-3.5" />New campaign</button></div></div>{message && <p className="rounded-md border border-primary/30 bg-primary/10 px-3 py-2 text-xs text-primary">{message}</p>}<div className="grid gap-3 sm:grid-cols-3"><Metric label="Active campaigns" value={String(campaigns.filter((x) => x.status === 'Active').length)} /><Metric label="Planned spend" value="$17.2k" /><Metric label="Channel coverage" value="5 / 5" /></div><div className="rounded-xl border border-border bg-card"><div className="border-b border-border px-5 py-4"><p className="font-mono text-[10px] uppercase tracking-[0.18em] text-muted-foreground">Campaign builder</p><h3 className="mt-1 text-sm font-semibold">Live campaign plan</h3></div><div className="divide-y divide-border">{campaigns.map((campaign) => <div key={campaign.name} className="flex flex-wrap items-center gap-4 px-5 py-4"><div className="flex size-9 items-center justify-center rounded-md bg-accent"><Megaphone className="size-4 text-primary" /></div><div className="min-w-44 flex-1"><p className="text-sm font-medium">{campaign.name}</p><p className="mt-1 text-xs text-muted-foreground">{campaign.budget} budget · {campaign.reach} projected reach</p></div><span className="rounded bg-accent px-2 py-1 font-mono text-[10px] text-primary">{campaign.status}</span></div>)}</div></div><div className="rounded-xl border border-border bg-card"><div className="flex items-center justify-between border-b border-border px-5 py-4"><div><p className="font-mono text-[10px] uppercase tracking-[0.18em] text-muted-foreground">Multi-channel planner</p><h3 className="mt-1 text-sm font-semibold">Content calendar</h3></div><span className="font-mono text-[10px] text-muted-foreground">MAY 2026</span></div><div className="grid grid-cols-5 border-b border-border">{days.map((day) => <div key={day} className="border-r border-border px-2 py-3 text-center font-mono text-[10px] text-muted-foreground last:border-r-0">{day}</div>)}</div><div className="grid min-h-44 grid-cols-5">{days.map((day) => <div key={day} className="border-r border-border p-2 last:border-r-0">{items.filter((item) => item.day === day).map((item) => { const channel = channels.find((c) => c.id === item.channel); const Icon = channel?.icon || Megaphone; return <div key={item.title} className="mb-2 rounded-md border border-primary/25 bg-primary/10 p-2"><Icon className="mb-2 size-3 text-primary" /><p className="text-[11px] font-medium leading-tight">{item.title}</p><p className="mt-1 text-[9px] uppercase text-muted-foreground">{channel?.label}</p></div> })}</div>)}</div></div>{campaignOpen && <Modal title="Create campaign" close={() => setCampaignOpen(false)}><form action={addCampaign} className="space-y-3"><input name="name" required placeholder="Campaign name" className="w-full rounded-md border border-border bg-background px-3 py-2 text-sm" /><textarea name="objective" placeholder="Objective" className="min-h-20 w-full rounded-md border border-border bg-background px-3 py-2 text-sm" /><input name="budget" type="number" min="0" placeholder="Budget" className="w-full rounded-md border border-border bg-background px-3 py-2 text-sm" /><button className="w-full rounded-md bg-primary px-3 py-2 text-sm font-medium text-primary-foreground">Save campaign</button></form></Modal>}{itemOpen && <Modal title="Plan content" close={() => setItemOpen(false)}><form action={addItem} className="space-y-3"><input name="title" required placeholder="Content title" className="w-full rounded-md border border-border bg-background px-3 py-2 text-sm" /><select name="channel" className="w-full rounded-md border border-border bg-background px-3 py-2 text-sm">{channels.map((c) => <option key={c.id} value={c.id}>{c.label}</option>)}</select><input name="scheduledFor" type="datetime-local" className="w-full rounded-md border border-border bg-background px-3 py-2 text-sm" /><button className="w-full rounded-md bg-primary px-3 py-2 text-sm font-medium text-primary-foreground">Add to calendar</button></form></Modal>}</section>
}
function Metric({ label, value }: { label: string; value: string }) { return <div className="rounded-xl border border-border bg-card p-4"><p className="font-mono text-[10px] uppercase tracking-widest text-muted-foreground">{label}</p><p className="mt-3 text-2xl font-semibold">{value}</p></div> }
function Modal({ title, close, children }: { title: string; close: () => void; children: React.ReactNode }) { return <div className="fixed inset-0 z-50 flex items-center justify-center bg-background/80 p-4 backdrop-blur-sm"><div className="w-full max-w-md rounded-xl border border-border bg-card p-6 shadow-2xl"><div className="mb-4 flex items-center justify-between"><h3 className="font-semibold">{title}</h3><button type="button" onClick={close} aria-label="Close" className="text-muted-foreground">×</button></div>{children}</div></div> }
